<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/add_score"
        android:title="Add Score"
        android:icon="@drawable/baseline_add_circle_24"
        app:showAsAction="ifRoom"/>

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/add_name"
        android:icon="@drawable/baseline_group_add_24"
        android:title="Add Name"/>

    <!-- Settings, should always be in the overflow -->


    <item android:id="@+id/clear_record"
        android:title="Clear Record"
        app:showAsAction="never"/>

</menu>